#include <stdio.h>
 
int main() {
  	int n, sum = 0, rem;
  	printf("25331A05G3\n");
  	printf("Enter a number: ");
  	scanf("%d", &n);
  	while (n != 0) {
        	rem = n % 10;
        	sum += rem;
        	n /= 10;
  	}
  	printf("Sum of digits = %d\n", sum);
  	return 0;
}
