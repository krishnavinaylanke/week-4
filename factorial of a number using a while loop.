#include <stdio.h>
 
int main() {
  	int n, i = 1;
  	long long fact = 1;
  	printf("25331A05G3\n");
  	printf("Enter a number: ");
  	scanf("%d", &n);
  	while (i <= n) {
        	fact *= i;
        	i++;
  	}
  	printf("Factorial of %d = %lld\n", n, fact);
  	return 0;
}
